{
  "name": "raisetimeline-perf",
  "private": true,
  "scripts": {
    "seed": "psql -h localhost -p 5432 -U raisetimeline -d raisetimeline_dev -f data/seed-perf-data.sql",
    "cleanup": "psql -h localhost -p 5432 -U raisetimeline -d raisetimeline_dev -f data/cleanup-perf-data.sql",
    "test:load": "k6 run scripts/load-test.js",
    "test:stress": "k6 run scripts/stress-test.js",
    "test:spike": "k6 run scripts/spike-test.js"
  },
  "devDependencies": {
    "k6-reporter": "^2.3.0"
  }
}
